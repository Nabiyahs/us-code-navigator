--- a/index.html
+++ b/index.html
@@ -517,8 +517,10 @@
 <!-- Top Search Bar -->
 <!-- Header height now matches sidebar header -->
 <header class="bg-white border-b border-gray-200 sticky top-0 shadow-sm z-20">
 <div class="px-8 py-4">
+<!-- PATCH: Wrap input in form for proper Enter key handling -->
+<form id="topSearchForm" class="max-w-2xl mx-auto search-input-wrapper" onsubmit="return false;">
 <div class="max-w-2xl mx-auto search-input-wrapper">
-<input class="search-input w-full px-4 py-3 pl-12 pr-24 border-2 border-gray-300 rounded-lg text-[#24305E] transition-all" id="topSearchInput" placeholder="Search codes, keywords, chapters..." type="text"/>
+<input class="search-input w-full px-4 py-3 pl-12 pr-24 border-2 border-gray-300 rounded-lg text-[#24305E] transition-all" id="topSearchInput" placeholder="Search codes, keywords, chapters..." type="search" autocomplete="off"/>
 <svg class="w-5 h-5 absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400" fill="none" stroke="currentColor" viewbox="0 0 24 24">
 <path d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"></path>
 </svg>
@@ -533,6 +535,7 @@
 </svg>
 </button>
 </div>
+</form>
 </div>
 </header>
 <!-- Home Section -->
@@ -42489,8 +42492,12 @@
 }

 // Search function for top search bar
 function performTopSearch() {
+    console.debug('[SEARCH] performTopSearch() called');
     const query = document.getElementById('topSearchInput').value.trim();
+    console.debug('[SEARCH] query:', query ? `"${query}"` : '(empty)');
     if (!query) return;

     // Switch to search results section (not advanced search)
@@ -42563,6 +42570,12 @@

     // Display results in search results section
     displayTopSearchResults(exactMatches, partialMatches, keyword);
+
+    // PATCH: Verify section visibility after render
+    const section = document.getElementById('searchResultsSection');
+    const computed = window.getComputedStyle(section);
+    console.debug('[SEARCH] searchResultsSection display:', computed.display,
+                  'visibility:', computed.visibility, 'hasActive:', section.classList.contains('active'));

     // Clear the top search input
     document.getElementById('topSearchInput').value = '';
@@ -42571,6 +42584,7 @@
 // Display top search results with detailed preview (with more content shown)
 function displayTopSearchResults(exactMatches, partialMatches, keyword) {
     const searchResultsSection = document.getElementById('searchResultsSection');
+    console.debug('[RENDER] displayTopSearchResults() - section exists:', !!searchResultsSection);
     if (!searchResultsSection) return;

     // Find or create results container
@@ -43351,34 +43365,71 @@
         });
     }

-    // Enter key in search inputs (with debug logging)
+    // PATCH: Primary handler - form submit (works with Enter key)
+    const topSearchForm = document.getElementById('topSearchForm');
+    if (topSearchForm) {
+        console.debug('[INIT] ✓ topSearchForm found, attaching submit listener');
+        topSearchForm.addEventListener('submit', function(e) {
+            console.debug('[EVENT] form submit fired');
+            e.preventDefault();
+            e.stopPropagation();
+
+            const input = document.getElementById('topSearchInput');
+            const query = input?.value.trim();
+            console.debug('[EVENT] submit - query:', query ? `"${query}"` : '(empty)');
+
+            if (query) {
+                performTopSearch();
+            }
+            return false;
+        });
+    } else {
+        console.warn('[INIT] ✗ topSearchForm NOT FOUND - form submit will not work!');
+    }
+
+    // PATCH: Backup handler - keydown on input (handles IME composition)
     const topSearchInput = document.getElementById('topSearchInput');
     if (topSearchInput) {
-        console.log('✓ topSearchInput event listener attached');
-        topSearchInput.addEventListener('keypress', function(e) {
-            console.log('topSearchInput keypress:', e.key);
+        console.debug('[INIT] ✓ topSearchInput found, attaching keydown listener');
+
+        // Use keydown (not deprecated keypress)
+        topSearchInput.addEventListener('keydown', function(e) {
+            console.debug('[EVENT] keydown fired - key:', e.key, 'code:', e.code, 'isComposing:', e.isComposing);
+
+            // CRITICAL: Ignore if IME composition in progress (Korean, Japanese, Chinese input)
+            if (e.isComposing || e.keyCode === 229) {
+                console.debug('[EVENT] IME composition active, ignoring');
+                return;
+            }
+
             if (e.key === 'Enter') {
-                console.log('Enter key pressed in topSearchInput');
+                console.debug('[EVENT] Enter key detected, calling performTopSearch()');
                 e.preventDefault();
+                e.stopPropagation();
                 performTopSearch();
             }
         });
     } else {
-        console.warn('✗ topSearchInput element not found');
+        console.warn('[INIT] ✗ topSearchInput NOT FOUND');
     }
+
+    // Advanced search input (same pattern)
     const keywordInput = document.getElementById('keywordInput');
     if (keywordInput) {
-        console.log('✓ keywordInput event listener attached');
-        keywordInput.addEventListener('keypress', function(e) {
-            console.log('keywordInput keypress:', e.key);
+        console.debug('[INIT] ✓ keywordInput found, attaching keydown listener');
+        keywordInput.addEventListener('keydown', function(e) {
+            console.debug('[EVENT] keywordInput keydown:', e.key, 'isComposing:', e.isComposing);
+
+            if (e.isComposing || e.keyCode === 229) {
+                console.debug('[EVENT] IME composition active, ignoring');
+                return;
+            }
+
             if (e.key === 'Enter') {
-                console.log('Enter key pressed in keywordInput');
+                console.debug('[EVENT] Enter in keywordInput, calling performSearch()');
                 e.preventDefault();
+                e.stopPropagation();
                 performSearch();
             }
         });
     } else {
-        console.warn('✗ keywordInput element not found');
+        console.warn('[INIT] ✗ keywordInput NOT FOUND');
     }

     // Initialize lucide icons
